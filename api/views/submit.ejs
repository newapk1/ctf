<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CTF - Submit Flag</title>
    <style>
        /* هەمان ستایلی CSSی پێشوو لێرە دابنێ */
        @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap' );
        :root { --bg-color: #1a1a1a; --form-bg: #2c2c2c; --text-color: #e0e0e0; --primary-color: #00ff99; --success-color: #28a745; --error-color: #ff4d4d; --border-color: #444; }
        body { font-family: 'JetBrains Mono', monospace; background-color: var(--bg-color); color: var(--text-color); display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        .container { background-color: var(--form-bg); padding: 2.5rem; border-radius: 8px; box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5); width: 100%; max-width: 500px; text-align: center; border: 1px solid var(--border-color); }
        h2 { color: var(--primary-color); margin-bottom: 1rem; }
        p { opacity: 0.8; }
        input { width: 100%; padding: 0.8rem; background-color: var(--bg-color); border: 1px solid var(--border-color); border-radius: 4px; color: var(--text-color); font-family: inherit; box-sizing: border-box; margin-top: 1.5rem; margin-bottom: 1.5rem; }
        button { width: 100%; padding: 0.8rem; border: none; border-radius: 4px; background-color: var(--success-color); color: white; font-size: 1rem; font-weight: 700; cursor: pointer; }
        .message { margin-top: 1.5rem; padding: 1rem; border-radius: 4px; font-weight: 700; }
        .success { background-color: rgba(0, 255, 153, 0.1); border: 1px solid var(--primary-color); color: var(--primary-color); }
        .error { background-color: rgba(255, 77, 77, 0.1); border: 1px solid var(--error-color); color: var(--error-color); }
        .back-link { display: inline-block; margin-top: 1.5rem; color: var(--primary-color); text-decoration: none; opacity: 0.8; }
        .back-link:hover { opacity: 1; }
        .fireworks { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 999; background-image: url('https://media.giphy.com/media/26tOZ42Mg6pbTUPHW/giphy.gif' ); background-repeat: no-repeat; background-position: center; background-size: cover; opacity: 0; transition: opacity 0.5s; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Submit Flag</h2>
        <p>Enter the secret message found in the admin's cookie to complete the challenge.</p>
        <form method="POST" action="/submit">
            <input type="text" name="flag" placeholder="CTF{...}" required>
            <button type="submit">Verify Flag</button>
        </form>

        <% if (message) { %>
            <div class="message <%= flag_correct ? 'success' : 'error' %>">
                <%= message %>
            </div>
        <% } %>

        <a href="/dashboard" class="back-link">[ Back to Dashboard ]</a>
    </div>

    <% if (flag_correct) { %>
        <div id="fireworks" class="fireworks" style="opacity: 1;"></div>
    <% } %>
</body>
</html>
